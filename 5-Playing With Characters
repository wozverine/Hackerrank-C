#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    char x;
    char y[100];
    char z[100];
    scanf("%c %s %[^\n]%*c", &x, &y, &z);
    printf("%c\n%s\n%s",x,y,z);
    return 0;
}
